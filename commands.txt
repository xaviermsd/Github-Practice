git init- initiliaze the directory
lcommc s -lart show hidden folders
git add - take my files to staging area
git add-A add all files
git commit
git commit name.fileName
git commit -m "your msg"
git checkout "filename"
git checkout -f for all restore the files
git restore --fileName
git log -p -1
git diff = "Shows diffren       qces from working tree"
git commit -a -m "Changed all files"
git rm --cached - removes from staging area only..removed from commit and make it tracked
git rm "Waste.html" removed from folder and branch
git status -s cheeck all the things
git .gitignore - ignore files for adding and commit
git "Write name of file" in .gitignore and also to remove all files *.log
Ingore git *.(Extension name) / for folder
q= to quit
Initial commit
esc+:wq to exitgit 
untracked-
staging area-
commit- 

//Branches
Main Branch= "Master"
Create Branch= git branch branch name
Switch Branch= git checkout branch name